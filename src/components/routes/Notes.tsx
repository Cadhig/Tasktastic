import { NotesSidebar } from "../NotesSidebar"
import { NotesMain } from "../NotesMain"
export default function Notes() {

    return (
        <div>
            <NotesSidebar />
            <NotesMain />
        </div>
    )
}